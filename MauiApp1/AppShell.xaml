<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MauiApp1.Views"
    x:Class="MauiApp1.AppShell">

    <TabBar>
        <ShellContent Title="Home"
                  ContentTemplate="{DataTemplate views:MainPage}" />
    <ShellContent Title="Detail"
                  Route="DetailPage"
                  ContentTemplate="{DataTemplate views:DetailPage}" /> 
        <ShellContent Title="Note"
                  Route="NotePage"
                  ContentTemplate="{DataTemplate views:NotePage}" />
        <ShellContent Title="Products" ContentTemplate="{DataTemplate views:ProductsPage}" />
    </TabBar>
</Shell>
